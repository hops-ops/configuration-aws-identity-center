name: on-pr

on:
  pull_request:
    branches:
      - main
    types:
      - labeled
      - opened
      - reopened
      - synchronize
    paths:
      - '.github/workflows/on-pr.yaml'
      - '.gitops/**'
      - 'apis/**'
      - 'examples/**'
      - 'tests/**'
      - 'functions/**'
      - 'upbound.yaml'

permissions:
  packages: write
  contents: write
  issues: write
  pull-requests: write

jobs:
  validate:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/validate.yaml@v2.5.1
    with:
      examples: |
        [
          { "example": "examples/identitycenters/minimal.yaml" },
          { "example": "examples/identitycenters/minimal.yaml", "observed": "examples/test/mocks/observed-resources/minimal/steps/1/" },
          { "example": "examples/identitycenters/minimal.yaml", "observed": "examples/test/mocks/observed-resources/minimal/steps/2/" },
          { "example": "examples/identitycenters/groups.yaml" },
          { "example": "examples/identitycenters/groups.yaml", "observed": "examples/test/mocks/observed-resources/groups/steps/1/" },
          { "example": "examples/identitycenters/groups.yaml", "observed": "examples/test/mocks/observed-resources/groups/steps/2/" },
          { "example": "examples/identitycenters/test-no-users.yaml" }
        ]
      api_path: apis/identitycenters
      error_on_missing_schemas: true

  test:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/test.yaml@v2.5.1

  e2e:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/e2e.yaml@v2.5.1
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    with:
      timeout-minutes: 40
      aws: true

  publish:
    needs:
      - validate
      - test
    uses: unbounded-tech/workflows-crossplane/.github/workflows/publish.yaml@v2.5.1
    secrets: inherit
    with:
      tag: pr-${{ github.event.pull_request.number }}-${{ github.sha }}
